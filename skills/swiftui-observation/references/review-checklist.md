# Review Checklist

- Does View remain primarily declarative rendering?
- Is mutable state centered in `@Observable` model?
- Is naming consistent with the scaffold reference?
- Is `design-docs/` updated if behavior/architecture changed?
- Are validation commands and outcomes documented in PR summary?
